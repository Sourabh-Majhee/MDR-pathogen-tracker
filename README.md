# MDR Pathogen Tracker
Tracks MDR pathogens in hospitals using FHIR.

## Setup
1. Clone repo
2. Backend: `cd backend && npm install && npm start`
3. Frontend: `cd frontend && npm install && npm start`
